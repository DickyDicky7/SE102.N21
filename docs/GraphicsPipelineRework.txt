[ignore this file]
1. game logic (virtual world) -> render

2. render:
2.1. resize (if needed) all textures/sprites and upload; from cpu->gpu once time at the start of the app; into 1 texture array
2.2. 1 ssbo contain data of every thing render in one single frame (1 single draw call each frame)
2.3.
#already provided:
- view-space (camera) transform matrix
- clip-space (projection/screen) transform matrix
struct <Data>
- world transform matrix
- 